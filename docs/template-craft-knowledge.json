{
  "meta": {
    "name": "TemplateCraft Knowledge Base",
    "version": "2.0.0",
    "description": "Structured documentation for TemplateCraft — an inference-based code generator that converts clean JSON Schema into Go Kart _template_builder documents",
    "lastUpdated": "2026-03-26"
  },
  "overview": {
    "description": "TemplateCraft is a deterministic, inference-based code generator for the Go Kart low-code framework. It takes a clean JSON Schema (from LiteSpec parseDSL(), no UI annotations) and generates complete _template_builder MongoDB documents by inferring the best Wave CSS components, layout, and structure from schema types and constraints.",
    "techStack": {
      "language": "JavaScript ES6+",
      "buildTool": "esbuild (with .njk loader plugin)",
      "templateEngine": "Nunjucks (custom delimiters: [% %], [= =])",
      "output": "IIFE bundle exposing window.templateCraft",
      "dependencies": ["nunjucks"]
    },
    "keyPrinciples": [
      "Schema describes data, the generator decides presentation — no UI annotations required",
      "Inference-based — examines type, format, enum, pattern, $ref, and property names to choose controls",
      "Deterministic — no LLM, no randomness, 100% predictable output",
      "Generates _template_builder documents with HTML content (Pongo2 markup)",
      "Nunjucks templates with custom delimiters avoid Pongo2 syntax conflicts"
    ]
  },
  "pipeline": {
    "description": "TemplateCraft sits at the end of the LiteSpec → Go Kart pipeline",
    "flow": [
      "1. LiteSpec DSL → parseDSL() → Clean JSON Schema",
      "2. JSON Schema → templateCraft.generate() → _template_builder documents (HTML content)",
      "3. Documents → POST to Go Kart API → MongoDB",
      "4. Go Kart serves the templates via /v/ and /x/ routes"
    ]
  },
  "api": {
    "generate": {
      "description": "Generate _template_builder documents from a JSON Schema",
      "signature": "generate(schema, options)",
      "params": {
        "schema": "Clean JSON Schema with properties, $defs, required, allOf, sort, breadcrumb",
        "options.collectionName": "MongoDB collection name (optional if schema.title is set)",
        "options.routePrefix": "'x' (protected) or 'v' (public), default 'x'",
        "options.templateType": "'tab-control' or 'card', default 'tab-control'",
        "options.functions": "Optional schema-level JavaScript functions",
        "options.includeList": "Generate list template, default true",
        "options.includeEdit": "Generate edit template, default true"
      },
      "returns": "Array of _template_builder document objects (typically 2: list + edit)",
      "example": "const templates = templateCraft.generate(schema, { collectionName: 'client', routePrefix: 'v' });"
    },
    "otherExports": {
      "generateListContent": "Generate HTML content for a list template",
      "generateEditContent": "Generate HTML content for an edit template",
      "generateEditCode": "Generate Code tab JavaScript for edit (runGet, runPut, runPost, runDelete)",
      "generateListCode": "Generate Code tab JavaScript for list (runList)",
      "generateFieldRules": "Generate field rules object from allOf conditionals",
      "generateFieldRulesString": "Generate field rules as JSON string",
      "generateEditForm": "Generate edit form HTML from normalized properties",
      "createTemplateDocument": "Create a _template_builder document with all fields",
      "inferControl": "Infer Wave CSS control from a schema property",
      "inferLayout": "Infer row/fieldset grouping from properties",
      "inferListCol": "Infer list column configuration from a property",
      "normalizeProperties": "Convert JSON Schema properties object to iterable array",
      "resolveRef": "Resolve a $ref to its definition from $defs",
      "findArrayTypesFromSchema": "Find primitive and non-primitive array fields"
    }
  },
  "inference": {
    "description": "The inference layer examines schema property characteristics to deterministically choose Wave CSS components and layout grouping. No UI annotations needed.",
    "controlInference": {
      "description": "Maps schema property types/constraints to Wave CSS components",
      "rules": [
        { "signal": "format: 'uuid' or ObjectId pattern", "control": "input-hidden" },
        { "signal": "type: 'boolean'", "control": "wc-input checkbox toggle-switch" },
        { "signal": "string + enum (2-3 values)", "control": "wc-input radio" },
        { "signal": "string + enum (4+ values)", "control": "wc-select dropdown" },
        { "signal": "format: 'email' or name contains 'email'", "control": "wc-input type=email" },
        { "signal": "format: 'date-time' or 'date'", "control": "wc-input type=date" },
        { "signal": "name contains 'phone' or 'tel'", "control": "wc-input type=tel" },
        { "signal": "name contains 'description', 'notes', 'bio', etc.", "control": "wc-textarea" },
        { "signal": "type: 'integer'", "control": "wc-input type=number step=1" },
        { "signal": "type: 'number' or Decimal128", "control": "wc-input type=currency" },
        { "signal": "array + items.type: 'string'", "control": "wc-select chip allow-dynamic" },
        { "signal": "array + items.$ref", "control": "fieldset-card with add/delete" },
        { "signal": "object + $ref", "control": "fieldset wrapping" },
        { "signal": "system fields (_id, created_*, modified_*)", "control": "skipped (meta_fields)" },
        { "signal": "string (fallback)", "control": "wc-input type=text" }
      ],
      "enumThreshold": "Enums with <= 3 values render as radio buttons; > 3 as select dropdowns",
      "systemFields": ["_id", "created_by", "created_date", "modified_by", "modified_date"]
    },
    "layoutInference": {
      "description": "Groups schema properties into responsive grid rows for form layout",
      "namePairHeuristics": [
        ["first_name", "last_name"],
        ["first_name", "middle_initial", "last_name"],
        ["email", "phone_number"],
        ["start_date", "end_date"],
        ["street", "city", "state", "postal_code"],
        ["city", "state", "postal_code"],
        ["latitude", "longitude"]
      ],
      "groupingStrategy": [
        "1. System fields excluded (handled by meta_fields)",
        "2. Known field pairs matched and grouped into rows",
        "3. Remaining fields grouped by common underscore prefix",
        "4. Ungrouped scalars rendered as singles",
        "5. Objects with $ref become fieldsets with recursive layout",
        "6. Arrays of objects become fieldset-cards with recursive sub-layout"
      ],
      "gridClass": "grid-1 sm:grid-2 md:grid-N gap-2"
    },
    "listColumnInference": {
      "description": "Determines list/table column configuration from property types",
      "rules": [
        { "signal": "first visible string", "result": "linkFormatter column (navigates to edit)" },
        { "signal": "subsequent strings", "result": "plain string column with header filter" },
        { "signal": "boolean", "result": "tickCross formatter with tristate header filter" },
        { "signal": "system fields", "result": "invisible columns" },
        { "signal": "objects, arrays", "result": "skipped (not shown in list)" },
        { "signal": "UUID, ObjectId", "result": "skipped entirely" }
      ]
    }
  },
  "outputFormat": {
    "description": "Generates _template_builder documents with Pongo2 HTML content",
    "collection": "_template_builder",
    "routes": "/v/{slug} (public), /x/{slug} (protected)",
    "baseTemplates": ["base", "loader", "partial-base"],
    "navigationFields": ["route_next_template_slug", "route_prev_template_slug"],
    "contentField": "content — Pongo2 HTML markup",
    "listFormatter": { "formatter": "linkFormatter", "formatterParams": "{\"routePrefix\": \"{{Template.RoutePrefix}}\", \"template\": \"{{Template.RouteNextTemplateSlug}}\", \"url\": \"urlFormatter\"}" },
    "contextVariables": "Template.RoutePrefix, Template.Slug, Template.RouteNextTemplateSlug, Template.RoutePrevTemplateSlug, Template.Schema, Template.CollectionName",
    "generatedParts": {
      "content": "Full Pongo2 HTML — extends __template_name__, block pageContent, wc-article-skeleton, breadcrumb, wc-form, inferred controls, wc-save-split-button, wc-hotkey, meta_fields include",
      "code": "runGet (CreateNew or FindByID), runPut/runPost (SaveAndValidate with array detection), runDelete (DeleteByID), runList (empty records array)",
      "field_rules": "JSON from allOf conditionals — visible, required, clearWhenHidden rules"
    }
  },
  "nunjucksTemplates": {
    "description": "HTML content is rendered via Nunjucks templates with custom delimiters to avoid Pongo2 conflicts",
    "customDelimiters": {
      "blockTags": "[% %] — Nunjucks if, for, block, etc.",
      "variableTags": "[= =] — Nunjucks variable interpolation",
      "commentTags": "[# #] — Nunjucks comments",
      "pongo2BlockTags": "{% %} — passes through as literal Pongo2 output",
      "pongo2VariableTags": "{{ }} — passes through as literal Pongo2 output"
    },
    "templateFiles": {
      "controls": [
        "wc-input-string.njk", "wc-input-email.njk", "wc-input-tel.njk", "wc-input-date.njk",
        "wc-input-boolean.njk", "wc-input-number.njk", "wc-input-integer.njk", "wc-input-checkbox.njk",
        "wc-input-radio-enum.njk", "wc-input-radio.njk", "wc-textarea.njk",
        "wc-select-enum.njk", "wc-select-lookup.njk", "wc-select-multiple.njk", "wc-select-multiple-dynamic.njk",
        "wc-select-multiple-collection.njk", "wc-select-screen-collection.njk", "wc-select-multiple-screen-collection.njk",
        "input-hidden.njk", "input-boolean.njk", "input-string.njk", "select.njk", "default.njk"
      ],
      "listCols": ["string.njk", "string-invisible.njk", "boolean.njk", "boolean-invisible.njk"],
      "generators": ["edit-content.njk", "list-content.njk", "code-edit.njk", "code-list.njk"],
      "editForm": ["fieldset.njk", "fieldset-card.njk", "fieldset-table.njk", "default-array.njk"],
      "layouts": ["card-prefix.njk", "card-suffix.njk", "tab-control-prefix.njk", "tab-control-suffix.njk"]
    },
    "customFilters": {
      "proper": "Converts snake_case to Proper Case (e.g., 'first_name' → 'First Name')"
    }
  },
  "inputSchemaFormat": {
    "description": "TemplateCraft accepts a clean JSON Schema produced by LiteSpec parseDSL() without UI annotations",
    "requiredFields": {
      "properties": "Object of property name → property schema",
      "title": "Used to derive collectionName if not explicitly provided (optional)"
    },
    "optionalFields": {
      "$defs": "Reusable type definitions referenced via $ref",
      "required": "Array of required field names",
      "allOf": "Conditional validation rules (if/then) — used to generate field_rules",
      "sort": "Default sort order array: [{ name, dir }]",
      "breadcrumb": "Breadcrumb display config: [{ name, suffix }]"
    },
    "propertySchema": {
      "type": "string, integer, number, boolean, object, array",
      "format": "email, date-time, date, uuid",
      "enum": "Array of allowed values",
      "pattern": "RegExp pattern string",
      "$ref": "Reference to $defs definition (e.g., '#/$defs/Address')",
      "items": "For arrays — { type: 'string' } or { $ref: '#/$defs/Member' }",
      "minimum": "Numeric minimum",
      "maximum": "Numeric maximum",
      "minLength": "String minimum length",
      "maxLength": "String maximum length",
      "default": "Default value",
      "anyOf": "LiteSpec nullable date-time pattern: [{ type, format }, { type, enum: [''] }]"
    },
    "example": {
      "title": "Client",
      "type": "object",
      "properties": {
        "first_name": { "type": "string", "minLength": 2, "maxLength": 50 },
        "last_name": { "type": "string", "minLength": 2, "maxLength": 50 },
        "email": { "type": "string", "format": "email" },
        "gender": { "type": "string", "enum": ["male", "female"] },
        "age": { "type": "integer", "minimum": 14, "maximum": 130 },
        "is_active": { "type": "boolean", "default": true },
        "tags": { "type": "array", "items": { "type": "string" } },
        "address": { "type": "object", "$ref": "#/$defs/Address" }
      },
      "required": ["first_name", "last_name", "gender"],
      "$defs": {
        "Address": {
          "type": "object",
          "properties": {
            "street": { "type": "string" },
            "city": { "type": "string" },
            "state": { "type": "string" },
            "postal_code": { "type": "string" }
          }
        }
      },
      "sort": [{ "name": "modified_date", "dir": "desc" }],
      "breadcrumb": [{ "name": "first_name", "suffix": " " }, { "name": "last_name" }]
    }
  },
  "sourceStructure": {
    "description": "Project file organization",
    "files": {
      "src/index.js": "Main entry — generate(), createTemplateDocument(), exports",
      "src/template-engine.js": "Nunjucks environment with custom delimiters, render(), registerTemplate()",
      "src/inference/infer-control.js": "Schema property → Wave CSS component inference",
      "src/inference/infer-layout.js": "Property grouping into grid rows, fieldsets, fieldset-cards",
      "src/inference/infer-list-col.js": "List column type/visibility inference",
      "src/inference/index.js": "Barrel export for inference module",
      "src/generators/edit.js": "Edit template HTML content generation",
      "src/generators/edit-form.js": "Recursive form HTML generation using inference + layout",
      "src/generators/list.js": "List template HTML content generation",
      "src/generators/code-tab.js": "Server-side JavaScript code generation (runGet, runPut, runPost, runDelete, runList)",
      "src/generators/field-rules.js": "Field rules from allOf conditionals (visible, required, clearWhenHidden)",
      "src/layouts/wrappers.js": "Card and tab-control layout wrappers for templates",
      "src/templates/register.js": "Imports and registers all .njk template files",
      "src/templates/controls/": "~23 Nunjucks control templates",
      "src/templates/list-cols/": "4 list column templates",
      "src/templates/generators/": "edit-content, list-content, code-edit, code-list templates",
      "src/templates/edit-form/": "fieldset, fieldset-card, fieldset-table, default-array templates",
      "src/templates/layouts/": "card and tab-control prefix/suffix templates",
      "src/utils/naming.js": "getProper, getSuffix, toSlug, quoteStrings",
      "src/utils/schema-helpers.js": "findProperty, normalizeProperties, resolveRef, findArrayTypes, findArrayTypesFromSchema"
    }
  },
  "buildAndTest": {
    "build": "npm run build — produces dist/template-craft.js (IIFE) and dist/template-craft.min.js",
    "release": "npm run release — builds then copies dist/ into dist/template-craft-{version}/",
    "testPage": "python3 -m http.server 3017 — view at http://localhost:3017/views/",
    "testButtons": [
      "Generate Article Templates",
      "Generate Client Templates"
    ]
  },
  "relatedProjects": {
    "goKart": {
      "path": "/Users/matthewduffield/Documents/_learn/go-kart/",
      "knowledgeBase": "/Users/matthewduffield/Documents/_learn/go-kart/docs/go-kart-knowledge.json",
      "description": "Low-code enterprise framework — consumes _template_builder documents, serves via /v/ and /x/ routes"
    },
    "waveCss": {
      "path": "/Users/matthewduffield/Documents/_dev/wave-css/",
      "knowledgeBase": "/Users/matthewduffield/Documents/_dev/wave-css/docs/wave-css-knowledge.json",
      "description": "Web component library — wc-input, wc-select, wc-tabulator, wc-live-designer, etc."
    },
    "liteSpec": {
      "path": "/Users/matthewduffield/Documents/_dev/lite-spec/",
      "knowledgeBase": "/Users/matthewduffield/Documents/_dev/lite-spec/docs/lite-spec-knowledge.json",
      "description": "DSL compiler — produces the clean JSON Schema that TemplateCraft consumes"
    }
  }
}
